<% provide(:title, 'Edit Data') %>

<div class="">
    <div class="page-title">
        <div class="title_left">
            <h3>Edit Data</h3>
        </div>
    </div>


<%= render 'form', project: @project, datum: @datum %>

<%= link_to 'Back', project_path(@project) %>

</div>
